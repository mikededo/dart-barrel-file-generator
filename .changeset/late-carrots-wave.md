---
'@dbfg/cli': patch
---

Simplify the CLI build to rely only on Vite and remove the failing externalization plugin from the build pipeline.
Upgrade related build dependencies as part of the CLI build fix.
